# Production requires env: JWT_SECRET, DB_URL, DB_USERNAME, DB_PASSWORD (no local defaults).
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
server.forward-headers-strategy=framework
